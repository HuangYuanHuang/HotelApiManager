@model HostelModel.WorkTypeModel
@addTagHelper "WebDirectiveExpand.Controls.GridBuilder,WebDirectiveExpand"
@{
    Layout = "_MainPage";
    ViewData["Title"] = "工种管理";
}
<crud-form asp-for="@Model" asp-url="@Url.Content("~/WorkType")" asp-icon="mif-users place-right" asp-title="工种列表" ng-controller="mainController" asp-form-double="false">

</crud-form>
@section Scripts{

    <script type="text/javascript">
        angular.module("app", ["ngBaseGrid"]).controller("mainController", function ($scope, BaseGrid) {
            $scope.Model = BaseGrid;
        })
        $("#work_config").addClass("active");
    </script>
}