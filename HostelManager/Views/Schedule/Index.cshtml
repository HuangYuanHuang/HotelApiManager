@model HostelModel.ScheduleModel
@addTagHelper "WebDirectiveExpand.Controls.GridBuilder,WebDirectiveExpand"
@{
    Layout = "_MainPage";
    ViewData["Title"] = "排班管理";
}
<crud-form asp-for="@Model" asp-url="@Url.Content("~/Schedule")" asp-icon="mif-users place-right" asp-title="排班列表" ng-controller="mainController" asp-form-double="false">

</crud-form>
@section Scripts{

    <script type="text/javascript">
        angular.module("app", ["ngBaseGrid"]).controller("mainController", function ($scope, BaseGrid) {
            $scope.Model = BaseGrid;
        })
        $("#schedule_config").addClass("active");
    </script>
}